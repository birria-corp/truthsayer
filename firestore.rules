rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /segments/{segmentId} {
      allow read: if true;
      allow write: if request.auth.uid == 'ZbRaWy2Ld5MPG0AtzyElphKWUiZ2';
    }
  }
}
